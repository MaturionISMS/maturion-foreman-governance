# This YAML file is for governance gate workflows

# Other contents of the file...

# Updated Loop
for dir in app components lib types tests foreman swarm; do
    # Command to run for each directory
    echo "Processing $dir"
    # ...rest of the script follows...

# Other contents of the file...