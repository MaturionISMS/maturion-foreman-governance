GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_INSTALLATION_ID=
GITHUB_WEBHOOK_SECRET=
OPENAI_API_KEY=
GITHUB_TOKEN=
# GitHub MCP (Model Context Protocol) Token
# REQUIRED for Foreman autonomy to function
# This token is used by the MCP server to access GitHub repositories
# Must have 'repo' scope for full repository access
# Should be a separate token from GITHUB_TOKEN for security
GITHUB_MCP_TOKEN=
# Foreman Behavior Files - MUST be loaded from governance repository
# Foreman governance lives at: maturion-ai-foreman/foreman/
# The Foreman App is only the supervisor, not the governance source
# Default configuration points to the governance repository
FOREMAN_BEHAVIOUR_REPO_OWNER=MaturionISMS
FOREMAN_BEHAVIOUR_REPO_NAME=maturion-ai-foreman
FOREMAN_BEHAVIOUR_DIR=foreman
# Local fallback: foreman/ directory (only for development/testing)
# Production MUST use the external governance repository
# Autonomous Mode Configuration
# FOREMAN_AUTONOMY_ENABLED enables Foreman to run without manual approval
# When true: Tasks are auto-approved, QA/compliance gates still enforced
# When false: All tasks require manual approval via /api/admin/approve
# DEFAULT RECOMMENDED: true for Maturion organization - QA controls risk
FOREMAN_AUTONOMY_ENABLED=true

# Legacy variables (deprecated, use FOREMAN_AUTONOMY_ENABLED instead)
# MATURION_AUTONOMOUS_MODE=true
# MATURION_ALLOW_AUTONOMOUS_BUILDS=true

# Autonomous Mode Safeguards (comma-separated: qa,compliance,tests)
# These gates are ALWAYS enforced, even in autonomous mode
MATURION_AUTONOMOUS_GUARDS=qa,compliance,tests

# Legacy variable name (deprecated, use MATURION_AUTONOMOUS_GUARDS instead)
# MATURION_AUTONOMOUS_SAFE_GUARDS=qa,compliance,tests

# Local Builder Paths (for pilot builds and local development)
# These are used when routing tasks to Local Builder
LOCAL_FOREMAN_APP_PATH=/Users/johan/.../maturion-foreman-app
LOCAL_PARTPULSE_PATH=/Users/johan/.../partpulse
LOCAL_ISMS_PATH=/Users/johan/.../MaturionISMS

# Builder Network Configuration
# Enable/disable builder network sync and detection
BUILDER_NETWORK_ENABLED=true
COPILOT_BUILDER_ENABLED=true
LOCAL_BUILDER_ENABLED=true

# Builder version (optional)
COPILOT_BUILDER_VERSION=latest

# Fallback configuration (already defined in local-builder.json)
# These can override config file settings via environment variables
# FALLBACK_ON_COPILOT_FAILURE=true
# FALLBACK_ON_TOKEN_EXHAUSTION=true  
# FALLBACK_ON_HIGH_COMPLEXITY=true
# PIPELINE_TIMEOUT_SECONDS=45
